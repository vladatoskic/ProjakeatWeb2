import { AbstractFilterParam } from './abstract-filter-param';

describe('AbstractFilterParam', () => {
  it('should create an instance', () => {
    expect(new AbstractFilterParam()).toBeTruthy();
  });
});
